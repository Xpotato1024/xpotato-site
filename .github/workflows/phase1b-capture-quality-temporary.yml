name: Temporary Phase 1B measurement patch

on:
  push:
    branches:
      - codex/site-vnext-legacy-visual-performance-baseline

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/site-vnext-legacy-visual-performance-baseline
      - name: Patch Phase 1B measurement quality
        run: python scripts/phase1b_capture_quality_temp.py
      - name: Commit measurement fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/site-validators/src/legacy-visual-performance-cli.ts
          if git diff --cached --quiet; then
            echo "No measurement patch needed."
            exit 0
          fi
          git commit -m "fix: Phase 1B measurement isolationを改善"
          git push origin HEAD:codex/site-vnext-legacy-visual-performance-baseline
