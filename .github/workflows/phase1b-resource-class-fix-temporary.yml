name: Temporary Phase 1B resource classification fix

on:
  push:
    branches:
      - codex/site-vnext-legacy-visual-performance-baseline

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/site-vnext-legacy-visual-performance-baseline
      - name: Patch resource classification
        run: python scripts/phase1b_resource_class_fix_temp.py
      - name: Commit resource classification fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/site-validators/src/legacy-visual-performance-cli.ts
          if git diff --cached --quiet; then
            echo "No resource classification patch needed."
            exit 0
          fi
          git commit -m "fix: Phase 1B resource分類を排他的にする"
          git push origin HEAD:codex/site-vnext-legacy-visual-performance-baseline
